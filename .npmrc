# Next.js 15 runs without sharp's native build locally; don't let pnpm's
# pre-run dependency check fail scripts over the intentionally-skipped build.
verify-deps-before-run=false
only-built-dependencies[]=sharp

