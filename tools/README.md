# Vami Workspace Tools & Utilities

This directory contains workspace-level developer scripts, internal tooling, and custom Nx code generators.

## Structure

- `tools/generators/`: Custom Nx code generators (e.g. scaffolding new domain modules following the `feature`, `data-access`, `domain`, `util` pattern).
- `tools/scripts/`: Workspace maintenance and automation scripts.
